# Project2
Đã tạo môi trường ảo .venv sẵn, không cần phải install lại 

Chỉnh lại đường dẫn (path) đúng trên file yaml và trên code. 

Có thể train lại và sử dụng folder big_dataset để train khối lượng to hơn 

Độ chính xác chưa cao, cần điều chỉnh lại cái tập dữ liệu xem có vấn đề gì không khớp hay không 
```
┌───────────────────────────────────────────────────────────────────────────────┐
│                                  FRONTEND (WEB APP)                           │
│───────────────────────────────────────────────────────────────────────────────│
│  - ReactJS / Next.js UI                                                       │
│  - Người dân upload video / ảnh tố cáo                                        │
│  - Cảnh sát theo dõi CCTV realtime                                            │
│  - Dashboard hiển thị kết quả AI (xe, biển số, loại vi phạm, khung thời gian) │
│  - Kết nối WebSocket để xem video CCTV và frame xử lý AI                      │
│                                                                               │
│   [1] Upload video → REST API                                                 │
│   [2] Nhận stream realtime → WebSocket (base64/MJPEG/WebRTC)                  │
│   [3] Nhận dữ liệu JSON (biển số, loại xe, vi phạm) → hiển thị UI             │
└──────────────────────┬────────────────────────────────────────────────────────┘
                       │ REST / WebSocket
                       │
┌──────────────────────▼────────────────────────────────────────────────────────┐
│                              BACKEND (FastAPI)                                │
│───────────────────────────────────────────────────────────────────────────────│
│  - Endpoint nhận video/ảnh tố cáo (POST /upload)                              │
│  - WebSocket server stream CCTV realtime (GET /ws/cctv)                       │
│  - Xử lý frame hoặc giao tiếp với module AI (YOLO, OCR, tracking)             │
│  - Tạo task nền (Celery/ThreadPool) để xử lý video dài                        │
│  - Ghi log vi phạm, lưu ảnh/video, trả kết quả JSON                           │
│                                                                               │
│   [4] Gửi frame sang module AI để xử lý                                       │
│   [5] Nhận kết quả AI (bbox, biển số, loại vi phạm)                           │
│   [6] Gửi frame đã annotate về WebSocket client                               │
│   [7] Lưu metadata → Database                                                 │ 
│   [8] Upload frame/video → Storage                                            │
└──────────────────────┬────────────────────────────────────────────────────────┘
                       │ Internal Queue / Async Call
                       │
┌──────────────────────▼────────────────────────────────────────────────────────┐
│                           AI MODULE (Python)                                  │
│───────────────────────────────────────────────────────────────────────────────│
│  - YOLOv8/YOLOv11: phát hiện xe & biển số                                     │
│  - OCR (EasyOCR / PaddleOCR): nhận dạng ký tự biển số                         │
│  - Tracking (SORT / DeepSORT): theo dõi xe giữa các frame                     │
│  - Violation Logic: kiểm tra vùng cấm / vượt đèn đỏ / tốc độ                  │
│  - Xuất kết quả JSON + frame đã annotate                                      │
│                                                                               │
│   [9] Trả kết quả JSON: {plate, type, time, camera_id, bbox, img_path}        │
└──────────────────────┬────────────────────────────────────────────────────────┘
                       │
┌──────────────────────▼────────────────────────────────────────────────────────┐
│                     DATABASE & STORAGE LAYER                                  │
│───────────────────────────────────────────────────────────────────────────────│
│  📦 PostgreSQL:                                                               │
│     - Bảng `violations` (id, plate, type, time, camera_id, evidence_url)      │
│     - Bảng `users`, `cameras`, `reports`                                      │
│                                                                               │
│  🗂️ MinIO / AWS S3:                                                           │
│     - Lưu video gốc, frame xử lý, ảnh vi phạm                                 │
│                                                                               │
│   [10] FastAPI ghi log và lưu đường dẫn file vào DB                           │
└──────────────────────┬────────────────────────────────────────────────────────┘
                       │
┌──────────────────────▼────────────────────────────────────────────────────────┐
│                       CCTV / VIDEO INPUT SOURCES                              │
│───────────────────────────────────────────────────────────────────────────────│
│  - IP Camera (RTSP, HTTP stream)                                              │
│  - Video tố cáo upload (mp4, avi, mov...)                                     │
│  - Camera quản lý giao thông trung tâm                                        │
│                                                                               │
│   [11] Stream → FastAPI AI pipeline                                           │
└───────────────────────────────────────────────────────────────────────────────┘
```
